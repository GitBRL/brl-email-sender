import { ComingSoon } from '../_components/coming-soon';

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Sending domain, default From identity, team members, unsubscribe page."
      phase="Phase 7"
    />
  );
}
